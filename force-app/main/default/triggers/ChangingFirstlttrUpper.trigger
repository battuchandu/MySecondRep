trigger ChangingFirstlttrUpper on Project__c (before insert) {
//ChanignToCaps.Change(trigger.new);
      //if(trigger.isBefore )
         //if(trigger.IsInsert ||trigger.IsUpdate )
           //ChanignToCaps.Change(trigger.new);
}